# pnpm reads build-script and other settings from here, not package.json's "pnpm" field.
# pnpm 11 removed onlyBuiltDependencies in favor of allowBuilds (a name -> allowed map).
allowBuilds:
  better-sqlite3: true
  esbuild: true
  sharp: true
  workerd: true
