# Force LF for all text files regardless of a contributor's core.autocrlf setting. Without this,
# Windows checkouts with core.autocrlf=true (the Git for Windows installer default) flag every
# file as modified in `git status` even when the content is byte-identical to HEAD.
* text=auto eol=lf
